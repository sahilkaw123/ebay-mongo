// Generated by CoffeeScript 1.10.0
(function() {
  var LogUtil,
    slice = [].slice;

  LogUtil = (function() {
    function LogUtil() {}

    LogUtil._fts = function(d) {
      return "[" + ((d != null ? d : new Date()).toISOString()) + "]";
    };

    LogUtil._fpid = function(p) {
      var ref;
      return "[p:" + ((ref = p != null ? p : process.pid) != null ? ref : '?') + "]";
    };

    LogUtil.tlog = function() {
      var args;
      args = 1 <= arguments.length ? slice.call(arguments, 0) : [];
      return console.log.apply(console, [LogUtil._fts()].concat(slice.call(args)));
    };

    LogUtil.terr = function() {
      var args;
      args = 1 <= arguments.length ? slice.call(arguments, 0) : [];
      return console.error.apply(console, [LogUtil._fts()].concat(slice.call(args)));
    };

    LogUtil.tplog = function() {
      var args;
      args = 1 <= arguments.length ? slice.call(arguments, 0) : [];
      return console.log.apply(console, [LogUtil._fts(), LogUtil._fpid()].concat(slice.call(args)));
    };

    LogUtil.tperr = function() {
      var args;
      args = 1 <= arguments.length ? slice.call(arguments, 0) : [];
      return console.error.apply(console, [LogUtil._fts(), LogUtil._fpid()].concat(slice.call(args)));
    };

    return LogUtil;

  })();

  exports.LogUtil = LogUtil;

}).call(this);
